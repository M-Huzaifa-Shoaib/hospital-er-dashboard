Place your final Excel workbook here, e.g. `Hospital_Emergency_Dashboard.xlsx`.

If the workbook contains any real/sensitive patient data, sanitize it first (replace with dummy names/IDs) before uploading — this repo is public.
